services:
  - type: web
    name: math-feedback-backend
    env: python
    buildCommand: "pip install -r math-feedback-backend-clean/requirements.txt"
    startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port 10000"
    plan: free
    region: oregon
